= Design: ShareShop
* Benjamin Banse Benjamin.Banse@stud.htw-dresden.de
* Kevin Bürger Kevin.Buerger@stud.htw-dresden.de
* Anni Nguyen Anni.Nguyen@stud.htw-dresden.de
* Emil Hempel Emil.Hempel@stud.htw-dresden.de
* Valencia Melvika Valencia.Melvika@stud.htw-dresden.de
* Florian Süßmann Florian.Suessmann@stud.htw-dresden.de
* Han Nguyen HanNguyen@stud.htw-dresden.de

{localtime}

include::../_includes/default-attributes.inc.adoc[]

== Zweck
Dieses Dokument hält wichtige Designentscheidungen für unsere App fest - vor allem, wie einzelne Module und Komponenten aufgebaut sind. Ziel ist, die technische Struktur übersichtlich darzustellen und den späteren Code leichter nachvollziehbar zu machen.

== Ausgewählte Sichten

=== Datenstruktur (ERM)
image:ShareShop_ERM.png[width=max, title="Entity-Relationship-Modell der Einkaufslistenanwendung"]

Das ERM zeigt die wichtigsten Datenobjekte wie Nutzer, WGs, Einkaufslisten und Items, und wie diese logisch miteinander verknüpft sind. Auch Änderungen und Benutzerzuordnungen sind als eigene Entitäten abgebildet.

=== Ablauf: Artikel hinzufügen (Sequenzdiagramm)
[plantuml]
----
@startuml
actor User
participant "Frontend" as FE
participant "Backend" as BE
participant "DB"

User -> FE : klickt auf "+ Artikel"
FE -> BE : POST /wg/{wid}/item
BE -> DB : INSERT INTO Items
DB --> BE : OK
BE --> FE : Antwort 201 Created
FE --> User : Artikel erscheint in der Liste
@enduml
----

Das Diagramm zeigt beispielhaft, wie ein Artikel in eine Einkaufsliste eingefügt wird, von der Nutzeraktion bis zur Datenbank.

=== User Interface (Wireframe)
Screens und Ideen fürs UI sind in der PDF unter folgendem Link zu finden:

link:../models/frontend/wireframe.pdf[Wireframe PDF anzeigen, window=_blank]

Die Figma-Entwürfe geben einen Eindruck davon, wie die App aussehen und sich bedienen lassen soll, inklusive Navigation, Kategorien und typischen Use Cases wie Listen erstellen, teilen oder Rezepte verwenden.

== Schnittstellen

Unsere App verwendet eine REST-API, um Frontend und Backend zu verbinden. Darüber laufen z.B. Login, WG-Verwaltung, Artikelsuche und das Bearbeiten von Einkaufslisten. Die API ist nach dem OpenAPI-3.0.4-Standard beschrieben und mit Javalin in Java umgesetzt.

=== Auswahl zentraler Endpunkte

Die Tabelle unten zeigt eine Auswahl der wichtigsten Schnittstellen. Die komplette Beschreibung steht in der Datei `rest.yaml`.

|===
| Methode | Pfad | Beschreibung

| POST  | /user/create               | Nutzer registrieren
| POST  | /user/login               | Login mit Session-Cookie
| GET   | /user                     | Daten des eingeloggten Nutzers
| POST  | /wg/create                | Neue WG erstellen
| GET   | /wg/{wid}                 | WG-Daten anzeigen
| POST  | /wg/{wid}/user            | Nutzer zur WG hinzufügen
| GET   | /wg/{wid}/list            | Alle Listen in einer WG abrufen
| POST  | /wg/{wid}/list            | Neue Liste anlegen
| GET   | /wg/{wid}/list/{lid}      | Liste anzeigen
| POST  | /wg/{wid}/list/{lid}      | Artikel zur Liste hinzufügen o. ä.
| GET   | /wg/{wid}/item            | Artikel suchen oder filtern
| POST  | /wg/{wid}/item            | Neuen Artikel erstellen
| PATCH | /wg/{wid}/item/{iid}      | Artikel bearbeiten
| DELETE| /wg/{wid}/item/{iid}      | Artikel löschen
| POST  | /wg/{wid}/category        | Neue Kategorie anlegen
|===

Fast alle Endpunkte benötigen eine gültige Session. Fehler wie „nicht eingeloggt“ oder „kein Zugriff“ werden mit den entsprechenden Statuscodes (`401`, `403`) zurückgegeben. Daten werden als JSON gesendet und empfangen, verarbeitet wird das mit der Bibliothek `gson`.

== Technischer Aufbau

Unsere Endpunkte sind mit Javalin umgesetzt. Jeder Bereich (z.B. Listen, Items) hat eine eigene Klasse, in der alle zugehörigen Routen registriert werden z.B. `ListEndpoints.java` oder `ItemEndpoints.java`. Die API prüft bei jeder Anfrage, ob der Nutzer eingeloggt ist und zur jeweiligen WG gehört.

Für Anfragen nutzen wir eigene Request-Objekte wie `CreateItemRequest.java` wird mit `gson` (de)serialisiert. Felder ohne `@Expose`-Annotation werden dabei ignoriert.

Ein typischer Ablauf für `POST /wg/{wid}/item` sieht so aus:

- Nutzer wird authentifiziert
- WG-Zugehörigkeit wird geprüft
- Request wird validiert
- Neues Item wird erstellt und als JSON zurückgegeben

Mehr Infos zum genauen Aufbau der Logik gibt es hier:
<<../models/backend_general/backend.adoc#, backend.adoc>>

=== Invite-System

Nutzer können einer WG nur über Einladungen hinzugefügt werden. Dafür wird ein Einladungscode generiert, welchen man dann als Link direkt teilen kann, oder durch einen QR-Code direkt scannen kann.

Ablauf:
```
Nutzer A                      Anwendung
    Invite für meine WG x bitte
  -------------------------------->
        Invite-Link
  <--------------------------------

Nutzer A schickt Link an Nutzer B

Nutzer B                      Anwendung
        Invite Link aufrufen
  -------------------------------->
                OK
  <--------------------------------
```

Detailliert
```
Client                        Backend
     POST create invite for WG
  ------------------------------->
          Invite Link/Error
  <-------------------------------


Client 2                      Backend
|---                               ---|
|      GET invite info for Link       |
|  ------------------------------->   |
|        Invite info/Error            |
|  <-------------------------------   |
|___                               ___|

      POST accept invite link
  ------------------------------->
            OK/Error
  <-------------------------------
```

=== Frontend-Architektur & Designentscheidungen

* **Framework & Build-Tool**
  HTML, CSS und Javascript mit Vue (2 & 3) framework für die Entwicklung der Anwendung.

* **Projektstruktur (`src/`)**
  - `App.vue`: Root-Komponente, zeigt `AuthWrapper`
  - `components/`:
    • `AuthWrapper.vue`: Umschaltung zwischen Login und Registrierung
    • `LoginForm.vue` & `RegisterForm.vue`: Formulare mit reaktiven Eingaben und Sprachumschaltung (DE/EN)

* **Formular- & Sprach-Logik**
  - `LoginForm.vue`:
    • `v-model` für Benutzername und Passwort
    • DE/EN-Toggle über Buttons
    • Event `switchMode` zum Wechsel der Formulare

* **Styling**
  - Komponenten-spezifisches Styling: Mehrere style.css-Dateien, die jeweils für spezifische Komponenten zuständig sind. Diese sind lokal und wirken sich nur auf die jeweilige Komponente aus.
  - Einheitliches Design: z. B. abgerundete Ecken, Schatten, mobile Breite.

* **Tooling & Dev-Workflow**
  - Entwicklung: `npm run start` → Startet den Live Server und öffnet die Anwendung im Browser.
  - Testen: Die Anwendung wird direkt im Browser über Live Server getestet.

